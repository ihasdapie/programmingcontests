#include <vector>
#include <map>
#include <algorithm>
#include <set>
#include <iostream>
#include <cstring>
#include <bits/stdc++.h> 
#define N 20000
#define loop(h) for(int i = 0; i < h; i++)
#define loop2(h) for(int j = 0; j < h; j++)




unordered_set<string> fillUnique(string s, int l){
	unordered_set<string> needs;

	loop(l){
		string ans = "";
		loop2(l){
			ans += s[j];
		

int main(){
	string needle;
	cin>>needle;
	
	string haystack;
	cin>>haystack
	
	fillUnique(needle, needle.size())
	
	
	
